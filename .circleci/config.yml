version: 2
jobs:
 build_and_test:
   machine: true
   steps:
     - checkout
     - run:
         name: Create settings
         command: |
           mv .env.dist .env
     - run:
         name: Run tests
         command:
           make test
workflows:
 version: 2
 build_and_test:
   jobs:
     - build_and_test
